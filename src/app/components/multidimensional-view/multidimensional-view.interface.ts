export type MultidimensionalArray = any[];

export interface Service {
    readonly title: string;
    readonly price: number;
}
  
export interface Node {
    readonly title: string;
    readonly icon: string;
}